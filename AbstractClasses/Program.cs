// See https://aka.ms/new-console-template for more information

/*using AbstractClasses.Game;

// Абстрактные классы нельзя использовать для создания экземпляра объекта
// AbstractClassTemplate abstractClassTemplate = new AbstractClassTemplate();

Warrior warrior = new Warrior(1)
{
    name = "Meow",
    level = 3
};
Wizard wizard = new Wizard(2);

warrior.Move();
warrior.UseAbility();
warrior.Attack();
warrior.Test();

wizard.Move();
wizard.UseAbility();
wizard.Attack();

warrior.PrintInfo();
wizard.PrintInfo();

Character[] characters = new Character[2];

characters[0] = new Wizard(24);
characters[1] = new Warrior(11);

foreach (var character in characters)
{
    character.Move();
    character.PrintInfo();
}*/
Console.WriteLine("Hello, World!");